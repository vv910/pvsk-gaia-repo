[project]
name = "pvsk-gaia-repo"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pvsk-gaia",
]

[tool.uv.workspace]
members = [
    "packages/*",
    "pvsk-gaia",
]
exclude = [
    "sources",
]

[tool.uv.sources]
pvsk-gaia = { workspace = true }
